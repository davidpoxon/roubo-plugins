id: claude-code
name: Claude Code
version: 0.1.0
description: Bundled agent plugin that launches Claude Code sessions with a configured model, effort, mode, and extra CLI arguments.
kind: agent
roubo: ^1.0.0
entry: ./dist/index.js
contractVersion: 1
configSchema:
  type: object
  properties:
    model:
      title: Model
      type: string
      default: default
      description: The Claude model the session runs on. `Account default` sends no `--model` flag and defers to the account setting.
      oneOf:
        - const: default
          title: Account default
        - const: opus
          title: Opus
        - const: sonnet
          title: Sonnet
        - const: haiku
          title: Haiku
    effort:
      title: Effort
      type: string
      default: high
      description: Reasoning effort passed as `--effort`.
      enum:
        - low
        - medium
        - high
        - xhigh
        - max
    mode:
      title: Mode
      type: string
      default: default
      description: Permission mode passed as `--permission-mode`. `Default` sends no flag and defers to the account setting.
      oneOf:
        - const: default
          title: Default
        - const: plan
          title: Plan
        - const: auto
          title: Auto
        - const: acceptEdits
          title: Accept edits
        - const: manual
          title: Manual
    extraArgs:
      title: Additional CLI arguments
      type: string
      description: Free-form extra arguments appended after the generated flags. Split into discrete argv tokens with quoting honoured; shell metacharacters are never interpreted.
permissions:
  network:
    hosts: []
  credentials:
    slots: []
  filesystem:
    paths: []
  # Declarative agent plugin: translateLaunch() only emits an AgentLaunchDescriptor
  # the host validates and executes, so the plugin spawns nothing itself. An agent
  # plugin must declare `processes: false`; the descriptor is its only write route.
  processes: false
  ports: false
  docker: false
