node_modules/
dist/
*.tsbuildinfo

# Test coverage output (generated by `npm run coverage`)
coverage/

# Release build/verify scratch output (generated tarballs + signed catalog)
release-build/
release-verify/

# Pages publish scratch output (signed catalog + key-ring) and derived public keys
pages-build/
*-pub.pem

# Local tooling state, never committed
.roubo/
.ship/
